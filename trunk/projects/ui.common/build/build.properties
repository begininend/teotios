FLEX_HOME = C:/Program Files/Adobe/Adobe Flash Builder 4 Plug-in/sdks/4.0.0

compc.exe = ${FLEX_HOME}/bin/compc.exe
asdoc.exe = ${FLEX_HOME}/bin/asdoc.exe

project.name = ui.common

ui.common.version = v1.0.0
project.name.version = ${project.name}-${ui.common.version}

project.swc = ${basedir}/bin/${project.name.version}.swc

bin.dir=${basedir}/bin
main.src.dir=${basedir}/src

deployed.dir=${basedir}/deployed
deployed.temp.dir=${basedir}/deployed-temp
doc.dir=${basedir}/doc